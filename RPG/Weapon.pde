class Weapon {
  String name;
  int damage;
  float price; // Price attribute for the weapon

  Weapon(String name, int damage, float price) {
    this.name = name;
    this.damage = damage;
    this.price = price; // Initialize the price
  }
}
